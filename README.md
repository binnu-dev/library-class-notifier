# Library Class Notifier

A Python application that monitors the Cheongcheon Library website for new classes and sends notifications to Discord.

## Setup

1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Create a `.env` file based on `.env.example` and add your Discord Webhook URL.
4. Run `python main.py`.
